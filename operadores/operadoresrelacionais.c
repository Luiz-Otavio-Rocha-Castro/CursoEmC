#include <stdio.h>
#include <stdlib.h>
        /*
            Precisamos aprender como fazer comparação com duas coisas na programação, para isso temos os operadores
            relacionais:
                        menor que (a<b)
                        maior que (a>b)
                        menor ou igual (a<=b)//toma cuidado na escrita pois nao pode usar =< / =>
                        maior ou igual (a>=b)
                        igual (a==b)
                        diferente (a != b)



        */
int main(){
    int a = 10, b = 20;
    printf("\n\tResultado: %d\n\n", a != b);
    // percebe-se que ao fazer essas relações ela sempre gera 1(verdade) ou 0(falso)


    return 0;
}
